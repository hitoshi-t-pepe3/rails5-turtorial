<% provide(:title, 'All users') %>
<p id="notice"><%= notice %></p>

<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>

<br>

<%= will_paginate %>


<%= link_to 'New User', new_user_path %>
